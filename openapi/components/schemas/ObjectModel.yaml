type: object
properties:
  custom_fields:
    type: array
    items:
      $ref: ./CustomFieldMetaData.yaml
  optional_properties:
    type: array
    description: >-
      These fields are not transmitted by default on this model, but can be
      requested by specifying them in a comma-separated list in the
      optional_properties query parameter.
    items:
      type: string
